name: Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches:
      - main

jobs:
  render-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js 21.x
        uses: actions/setup-node@v4
        with:
          node-version: 21.x

      - name: Install Deps
        run: npm install

      - name: Run Build
        run: npm run build

      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          component: true

      - name: Deploy
        if: github.ref == 'refs/heads/main'
        env:
          DEPLOY_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          curl "$deploy_url"
# curl is used to trigger the deployment on Render. The URL is stored in GitHub secrets for security.
          # Make sure to replace $deploy_url with the actual secret name if different.
          # Example: curl "$RENDER_DEPLOY_HOOK_URL"